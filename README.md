# Welcome to CSE341 Assignment 2.

## Getting Started

- Clone the repository to your local machine.
- Change directory into the cloned Repo and open your text editor.
- Set up your config as shown in the .example.env file in the config folder.
- Open your terminal and type `npm install` then `npm run dev`.
